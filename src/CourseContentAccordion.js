import React, {useState} from "react";

export default function CCA(props) {

    const {wilt, mfca} = props;

    return (
        <div style={{width: "100%", paddingLeft: "5%", paddingRight: "5%"}}>
            
        </div>
    );
}